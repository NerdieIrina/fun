---
title: Default title
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}
    </title>
  </head>
  <body>
    {{ content }}

    {% for experience in collection.experiences %}
      <p>
        <a href="{{ experience.url }}">{{ experience.data.title }}</a>
      </p>
    {% endfor %}
  </body>
</html>