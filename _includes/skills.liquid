<section class="border--bottom">
  <h2>Skills</h2>
  <ul class="skills">
    {% for skill in collections.skills %}
      <li>
        <span class="pill">{{ skill }}</span>
      </li>
    {% endfor %}
  </ul>
</section>